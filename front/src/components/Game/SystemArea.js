import React, { Component, PropTypes } from 'react';

const propTypes = {
};

const defaultProps = {
};

class SystemArea extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return(
            <div>SystemArea</div>
        );
    }
}

SystemArea.propTypes = propTypes;
SystemArea.defaultProps = defaultProps;

export default SystemArea;
