import React, { Component, PropTypes } from 'react';

const propTypes = {
};

const defaultProps = {
};

class GameResult extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return(
            <div>GameResult</div>
        );
    }
}

GameResult.propTypes = propTypes;
GameResult.defaultProps = defaultProps;

export default GameResult;
