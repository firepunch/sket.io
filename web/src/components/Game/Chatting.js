import React, { Component, PropTypes } from 'react';

const propTypes = {
};

const defaultProps = {
};

class Chatting extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return(
            <div id="sket-chatting">
                chatting
            </div>
        );
    }
}

Chatting.propTypes = propTypes;
Chatting.defaultProps = defaultProps;

export default Chatting;
